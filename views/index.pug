extends layout


block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
    br
    a(href="/content") See your content!
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/content") See your content!
    br
    a(href="/logout") Logout